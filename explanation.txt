Two-pass stream: hashed and partitioned by (claim_id,status_code) into temp buckets (files) to bound memory.
Bucket counts are configurable and opened lazily to avoid OS file limits.
Process each bucket, build graphs per group, and use DFS to find the longest simple cycle.
Track the global max.
Future: add SCC pruning and parallel bucket processing.

- Jeff Lee